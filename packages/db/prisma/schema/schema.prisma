generator client {
  provider = "prisma-client"
  output   = "../generated"
  moduleFormat = "esm"
  previewFeatures = ["relationJoins", "postgresqlExtensions"]
  compilerBuilder = "small"
}

datasource db {
  provider = "postgresql"
  extensions = [vector]
}
